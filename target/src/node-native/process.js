// Generated by CoffeeScript 1.8.0
(function() {
  var child_process,
    __slice = [].slice;

  child_process = require('child_process');

  String.prototype.start_Process = function() {
    var args;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    if (args == null) {
      args = [];
    }
    if (args.first() instanceof Array) {
      args = args.first();
    }
    return child_process.spawn(this.str(), args);
  };

  String.prototype.start_Process_Redirect_Console = function() {
    var args, childProcess;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    if (args == null) {
      args = [];
    }
    childProcess = this.start_Process(args);
    childProcess.stdout.on('data', function(data) {
      return console.log(data.str().trim());
    });
    childProcess.stderr.on('data', function(data) {
      return console.log(data.str().trim());
    });
    return childProcess;
  };

  String.prototype.start_Process_Capture_Console_Out = function() {
    var args, call_Callback, callback, childProcess, closed, consoleData, exited, _i;
    args = 2 <= arguments.length ? __slice.call(arguments, 0, _i = arguments.length - 1) : (_i = 0, []), callback = arguments[_i++];
    closed = false;
    exited = false;
    call_Callback = function() {
      if (closed && exited) {
        return process.nextTick(function() {
          return callback(consoleData);
        });
      }
    };
    consoleData = "";
    childProcess = this.start_Process(args);
    childProcess.stdout.on('data', function(data) {
      return consoleData += data;
    });
    childProcess.stderr.on('data', function(data) {
      return consoleData += data;
    });
    childProcess.on('close', function() {
      closed = true;
      return call_Callback();
    });
    childProcess.on('exit', function() {
      exited = true;
      return call_Callback();
    });
    return childProcess;
  };

}).call(this);
